services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tunnel-gmao-frontend
    ports:
      - "8082:80"
    restart: unless-stopped
    environment:
      # Variables d'environnement si nécessaire (ex: API_URL via injection au runtime)
      - NODE_ENV=production
